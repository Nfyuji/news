# 📰 بوت نشر الأخبار الآلي على تليجرام
# Telegram Auto News Publisher Bot

بوت بايثون يجلب الأخبار من مصادر RSS متعددة وينشرها تلقائياً في قناتك على تليجرام.

---

## 🌟 المميزات

✅ يعمل 24/7 على السيرفر مجاناً  
✅ يدعم مصادر متعددة (6 مصادر عربية مضافة افتراضياً)  
✅ يمنع التكرار تلقائياً  
✅ تنسيق جميل مع إيموجيز وهاشتاجات  
✅ سهولة التعديل والإضافة  
✅ قاعدة بيانات SQLite للتتبع  

---

## 📁 هيكل الملفات

```
Telegram/
├── news_bot.py          # البرنامج الرئيسي
├── config.py            # إعدادات المصادر والتنسيق
├── requirements.txt     # مكتبات بايثون
├── .env.example         # نموذج الإعدادات السرية
├── .env                 # الإعدادات السرية (تصنعه أنت)
├── Procfile             # للنشر على السيرفر
└── README.md            # هذا الملف
```

---

## 🚀 التشغيل السريع

### 1️⃣ الحصول على توكن البوت

1. افتح تليجرام وابحث عن `@BotFather`
2. أرسل `/newbot`
3. اختر اسم للبوت
4. احفظ التوكن الذي يعطيه لك

### 2️⃣ إضافة البوت للقناة

1. أضف البوت كـ **Admin** في قناتك
2. أعطه صلاحية **Post Messages**

### 3️⃣ إعداد ملف .env

انسخ ملف `.env.example` إلى `.env`:
```bash
copy .env.example .env
```

ثم عدل القيم:
```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_ID=@your_channel_name
CHECK_INTERVAL_MINUTES=30
```

### 4️⃣ تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 5️⃣ تشغيل البوت

```bash
python news_bot.py
```

### 6️⃣ تشغيل لوحة التحكم (ويب)

لتشغيل واجهة الإدارة:
```bash
python web_app.py
```
ثم افتح المتصفح على: `http://localhost:5000`

**بيانات الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

---

## ⚙️ الإعدادات

### لوحة التحكم (جديد 🔥)
يمكنك الآن إدارة كل شيء من خلال المتصفح:
1. مشاهدة الإحصائيات (عدد الأخبار والمصادر)
2. إضافة/حذف مصادر RSS بسهولة
3. مشاهدة آخر الأخبار المنشورة
4. تغيير الإعدادات دون لمس الكود

### ملف config.py (للطوارئ)
يستخدم كمصدر احتياطي في حال عدم وجود قاعدة بيانات.


### إضافة مصادر جديدة
```python
RSS_FEEDS = {
    "اسم المصدر": "رابط RSS",
    # أضف المزيد...
}
```

### تغيير الحد الأقصى للأخبار
```python
MAX_POSTS_PER_CHECK = 3  # عدد الأخبار في كل مرة
```

### تغيير فترة عدم التكرار
```python
NEWS_COOLDOWN_HOURS = 6  # ساعات
```

---

## ☁️ النشر على السيرفر (مجاني)

### Render.com
1. سجل حساب في [render.com](https://render.com)
2. أنشئ **Background Worker**
3. اربطه مع GitHub أو ارفع الملفات
4. أضف Environment Variables
5. Start!

### Railway.app
1. سجل في [railway.app](https://railway.app)
2. أنشئ مشروع جديد
3. ارفع الملفات
4. أضف المتغيرات البيئية
5. Deploy!

---

## 📊 قاعدة البيانات

البوت يصنع ملف `news_bot.db` تلقائياً يحفظ:
- جميع الأخبار المنشورة
- يمنع التكرار
- يحذف الأخبار القديمة (أكثر من 7 أيام) تلقائياً

---

## 🔧 استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| `TELEGRAM_BOT_TOKEN` error | تأكد من نسخ التوكن بشكل صحيح |
| `Chat not found` | تأكد أن البوت Admin في القناة |
| `No news fetched` | تحقق من روابط RSS في config.py |

---

## 📝 ملاحظات

- هذا البوت للاستخدام الشخصي والتعليمي
- للتوسع تجارياً، انتبه لحقوق النشر
- يمكن تعديل الكود حسب احتياجاتك

---

## 📞 المساعدة

إذا واجهت أي مشكلة، تحقق من:
1. صحة التوكن في ملف `.env`
2. أن البوت مدير في القناة
3. أن روابط RSS تعمل

---

Made with ❤️ for Arabic News
